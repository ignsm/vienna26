# Copy to .env.local and fill in. Never commit .env.local.
# Get a free Neon connection string at https://neon.tech (takes ~1 minute).
DATABASE_URL="postgres://user:password@host/dbname?sslmode=require"
